# 质量认证图片

将三张认证证书图片放入本目录，并命名为：

- **cert-iso9001.png** — 质量管理体系认证证书（ISO 9001 / YAB）
- **cert-machinery.png** — 机械指令符合性证明（CE / UDEM）
- **cert-software.png** — 计算机软件著作权登记证书（国家版权局）

支持 `.png` 或 `.jpg`，若使用 `.jpg` 请在 `web/app/about/page.tsx` 的 `qualityCertifications` 中把对应 `image` 改为 `.jpg`。
